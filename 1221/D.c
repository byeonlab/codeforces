#include<stdio.h>
#include<stdlib.h>

int increase(int *a, int *b, int from, int n, int cost){
    for(int 
}

int main(){

    int q;
    scanf("%d", &q);

    while(q--){
        int n;
        int *a, *b;

        scanf("%d", &n);

        a = malloc(sizeof(int) * n);
        b = malloc(sizeof(int) * n);

        min[i] = min +
    }


